import styled from "styled-components"

const HoverText = styled.ul`
	color: #FFFFFF;
	font-weight: 400;
	font-size: 20px;
	:hover {
		cursor: pointer;
		color: #B9C0D6;
	}
  `

export default HoverText